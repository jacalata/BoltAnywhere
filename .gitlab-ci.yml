pages:
  stage: deploy
  script:
  - mkdir .public
  - cp -r * .public
  - mv .public public
  artifacts:
    paths:
    - public
  only:
  - master

 
# command to install dependencies
"pip install -r requirements.txt"
# command to run tests
nosetests tests/proof_of_concept.py